import * as React from 'react';
import { BrowserRouterProps } from 'react-router-dom';

const Home: React.StatelessComponent<BrowserRouterProps> = () => (
	<div>
		<h2>Home</h2>
	</div>
);

export default Home;
